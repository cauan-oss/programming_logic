const caixa_de_palavras = ["u", "j", "x", "g", "b", "e"];
let valores = [];
const guardaDentroArray = [];
for(let index = 0; index < caixa_de_palavras.length; index += 1) {
   valores[index] = caixa_de_palavras[index]
   if (valores[index] === "b") {
    console.log(valores[index])
   }
   else if (valores[index] === "u"){
    console.log(valores[index])
   }
   if(valores[index] === "g") {
    console.log(valores[index])
   }
}

//console.log(valores)